<!-- Views/LoginView.xaml -->
<Window x:Class="ATMApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="350">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Numéro de Carte" Margin="0,0,0,5"/>
            <TextBox x:Name="CardNumberTextBox" Margin="0,0,0,10"/>
            <TextBlock Text="Code PIN" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PinCodeBox" Margin="0,0,0,10"/>
            <Button Content="Login" Click="OnLoginClick" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Window>